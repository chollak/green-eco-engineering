# GREEN ECO ENGINEERING — сайт

Статический двуязычный (RU/UZ) сайт электромонтажной компании.
20 страниц: главная, каталог работ, 17 страниц направлений, 404.

**Продакшн:** https://chollak.github.io/green-eco-engineering/
(GitHub Pages, ветка `main`, корень репозитория)

## Сборка

```bash
node build.mjs
```

HTML в корне — **артефакт сборки**. Править его руками бессмысленно: перезапишется.
Сборка падает, если в разметку просочился абсолютный путь `/...` — на Pages сайт живёт
по подпути, такие ссылки ломаются.

## Где что править

| Что | Файл |
|---|---|
| Направления и виды работ (17 / 109) | `src/data/sections.mjs` |
| Реквизиты, обязательства, отрасли | `src/data/company.mjs` |
| Тексты главной (hero, доверие, процесс, FAQ) | `src/data/content.mjs` |
| SEO, выгоды, FAQ по разделам | `src/data/enrichment.mjs` |
| Кластеры каталога | `src/data/groups.mjs` |
| Стили | `src/assets/styles.css` |
| Скрипты | `src/assets/site.js` |
| Разметка | `src/templates/*.mjs` |
| Термины RU→UZ | `docs/glossary.md` |

Каждый текст — пара `{ ru, uz }`. Переключатель языка подменяет содержимое из `data-uz`
без перезагрузки, выбор хранится в `localStorage`.

**Узбекские апострофы:** `o‘`/`g‘` — U+2018, `ta’mir` — U+2019.
Нормализуются скриптом:

```bash
node scripts-normalize-apostrophes.mjs
```

Орфографически верны U+02BB и U+02BC, но в IBM Plex Sans их глифы нарисованы
как модификаторы шириной 24px при кегле 40 — шире буквы «o» (22.4px). Слово
`To‘liq` в них набирается как `To ʻ liq`. U+2018 в этой же гарнитуре — 10.92px
и правильной формы. При смене гарнитуры замер стоит повторить.

**Стрелки и галочки** — только inline SVG из `src/templates/icons.mjs`:
глифов `→` и `✓` нет ни в IBM Plex Sans, ни в IBM Plex Mono.

## Дизайн

Направление «ПРОТОКОЛ 01–17»: страница верстается как лист исполнительной документации —
белая бумага с зелёным подтоном, hairline-линии, сквозная моноширинная нумерация,
однолинейная схема вместо фотографий, фактура в формате «параметр : значение».

Правила, которые легко нарушить при доработке:
- фотографий на сайте нет и не будет — это жанр, а не дефицит;
- инверсия ровно одна (секция `#contact`), чередования полос быть не должно;
- радиусов три: `0`, `2px` (чипы), `6px` (кнопки и поля). Единый радиус на всём — маркер шаблона;
- вертикальные отступы секций — три разных токена, не один;
- рамки карточек — `box-shadow: inset 0 0 0 1px`, не `border`;
- бесконечных анимаций нет ни одной; бары анимируются `transform`, не `width`.

## Требует заполнения клиентом

Поля со значением `null` в `src/data/company.mjs` намеренно не выдуманы —
соответствующие блоки просто не выводятся, пока их не заполнят:

- `bank` — банковские реквизиты для договора;
- `labCertificate` — номер и дата свидетельства о регистрации электролаборатории;
- `staff` — сколько сотрудников с группами III / IV / V и сколько бригад;
- `telegram` — ссылка на Telegram, если он есть.

## Требует подтверждения клиентом — до публикации

Тексты содержат конкретные проверяемые обязательства. Если хотя бы одно не соответствует
действительности, это репутационный и юридический риск: сайт обещает от имени компании.

**Коммерческие сроки и гарантии**
- смета и коммерческое предложение за 1–2 рабочих дня;
- выезд электролаборатории за 24 часа по Ташкенту;
- гарантия на работы 24 месяца с даты акта;
- аварийная служба 24/7 и выезд по Ташкенту в течение часа;
- цена фиксируется в договоре и не меняется по инициативе исполнителя.

**Допуски и квалификация**
- собственная электролаборатория с правом выдачи протоколов: номер и дата свидетельства,
  **область измерений** (какие испытания и до какого класса напряжения разрешены), даты поверки приборов;
- работы до 110 кВ — на сайте это заявлено в девяти местах, но все конкретные примеры
  не выходят за 10 кВ. Либо подтвердить документами, либо снизить заявление до фактического;
- персонал с группами по электробезопасности до V, работы по наряду-допуску;
- участие в закупках на xarid.uzex.uz и казначейское финансирование.

**Формулировки для сверки**
- действующее название надзорного органа (в текстах — «Государственная инспекция по надзору
  в электроэнергетике / Узгосэнергонадзор», 7 вхождений);
- приборы и методы, названные поимённо в текстах разделов;
- технологические сроки по каждому разделу в `src/data/enrichment.mjs`.

## Известное ограничение: узбекская версия и поиск

Узбекский текст лежит в атрибутах `data-uz` и подставляется скриптом — поисковик видит
только русский. Отдельных URL и `hreflang` нет. Это осознанный размен: без сборщика и сервера
второй языковой набор страниц удваивает объём генерации и ссылочную структуру.

Если узбекский трафик из поиска понадобится, минимальный вариант — генерировать второй
комплект страниц в `/uz/` из тех же данных (шаблоны это уже позволяют: язык передаётся
одним параметром) и связать пары через `hreflang`.

## Не подключено

Форма заявки валидирует поля и показывает подтверждение, но никуда не отправляет —
см. `TODO` в `src/assets/site.js`. Нужен Telegram Bot API, почтовый шлюз или Formspree.

## Картинка для соцсетей

`assets/og-image.png` генерируется скриншотом страницы `og-image.html` в размере 1200×630.
После изменения текстов на ней пересними и положи результат в `src/assets/og-image.png`.
